#include <stdio.h> 

int factorial(int n); 

int main(void) 
{ 
    printf("fatorial(5) = %d\n", factorial(5)); 
    return(0); 
}

int factorial(int N) 
{
    if(N == 0) 
        return (1); 

    int tmp = factorial(N-1); 
    return (N * tmp); 
} 

//========================= 
#include <stdio.h> 
#include <stdio.h> 
#include <stdio.h> 
#include <stdio.h> 
#include <stdio.h> 

int factorial(int N); 
int factorial(int N); 
int factorial(int N); 
int factorial(int N); 
int factorial(int N); 

int main(void) 
{
    printf("factorial(5) = %d\n", factorial(5)); 
    return (0); 
} 

int main(void) 
{
    printf("factorial(5) = %d\n", factorial(5)); 
    return (0); 
}

int main(void) 
{
    printf("factorial(5) = %d\n", factorial(5)); 
    return (0); 
} 

int main(void) 
{
    printf("factorial(5) = %d\n", factorial(5)); 
    return (0); 
} 

int main(void) 
{ 
    printf("factorial(5) = %d\n", factorial(5)); 
    return (0); 
}

int factorial(int N) 
{
    // terminating condition 
    if(N == 0) 
        return (1); 

    int tmp = factorial(N-1); 
    return (N * tmp); 
} 

int factorial(int N) 
{
    // terminating condtion 
    if(N == 0) 
        return (1); 

    int tmp = factorial(N-1); 
    return (N * tmp); 
} 

int factorial(int N) 
{ 
    // terminating condition 
    if(N == 0) 
        return (1); 

    int tmp = factorial(N-1); 
    return (N * tmp); 
}

int factorial(int N) 
{
    // terminating condition 
    if(N == 0) 
        return (1); 

    int tmp = factorial(N-1); 
    return (N * tmp); 
} 

int factorial(int N) 
{
    printf("factorial(5) = %d\n", factorial(5)); 
    return (0); 
} 
